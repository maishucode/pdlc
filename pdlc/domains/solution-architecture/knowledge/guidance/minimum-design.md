# Minimum sufficient solution design

Document system boundaries, dependencies, data flow, failure behavior, material trade-offs, and decisions that would be costly to reverse. The design should support a delivery decision; it should not become ceremony detached from risk.
