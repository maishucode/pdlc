{
  "schemaVersion": 1,
  "id": "security.credential-boundary",
  "title": "Credential boundary",
  "description": "Credentials are never embedded in implementation or delivery artifacts.",
  "ownerDomain": "security",
  "version": "1.0.0",
  "appliesTo": {
    "deliveryFlows": ["poc", "implementation", "pdlc"],
    "stages": ["data-integration-boundaries", "solution-design", "build-readiness", "implementation", "developer-verification", "security-verification", "deployment"]
  },
  "rules": [
    {
      "id": "no-embedded-secret",
      "statement": "Keep credentials out of code, prompts, Delivery Records, logs, and evidence artifacts; use an approved secret mechanism when credentials are required.",
      "enforcement": "evidence",
      "requiredEvidence": ["Secret handling design or evidence that no credentials are used"],
      "exceptionApprovers": ["security-governance"],
      "standardDefault": { "key": "security.credential-boundary", "topic": "dataIntegrations" }
    }
  ]
}
