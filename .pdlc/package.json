{
  "name": "lean-pdlc-harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "pdlc": "bun cli.ts --root ..",
    "test": "bun test ./tests/*.test.ts",
    "test:e2e": "bun test ./tests/framework-e2e.test.ts",
    "typecheck": "tsc --project tsconfig.json",
    "validate": "bun cli.ts validate --root .."
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
