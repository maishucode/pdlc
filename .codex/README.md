# Optional Codex Adapter

Codex currently discovers the shared `AGENTS.md` and `.agents/skills/lean-pdlc/` sources directly, so no Codex-specific Delivery Flow definition is required.

This folder is reserved for thin Codex-only capability or approval mapping. It must not contain copied Stages, Delivery Flows, Domain Controls or Knowledge, roles, schemas, checkpoints, or delivery state.
