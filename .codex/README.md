# Optional Codex Adapter

Codex currently discovers the shared `AGENTS.md` and `.agents/skills/lean-pdlc/` sources directly, so no Codex-specific workflow definition is required.

This folder is reserved for thin Codex-only capability or approval mapping when a later phase requires it. It must not contain copied Stages, User Journeys, workflows, gates, roles, Principle Packs, schemas, or delivery state.
